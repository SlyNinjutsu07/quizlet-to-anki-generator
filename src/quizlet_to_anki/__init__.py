"""quizlet_to_anki — a tool that turns a Quizlet study set into an Anki .apkg deck.

This is a "package marker" file. Its presence tells Python that the folder
`quizlet_to_anki/` is an importable package, so other files can do things like
`from quizlet_to_anki.converter import build_deck`.

It can stay almost empty. A version string is a common thing to put here.
"""

__version__ = "0.1.0"
