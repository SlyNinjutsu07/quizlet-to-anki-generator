"""test_converter.py — prove the converter half works, offline, with fake cards.

This is where you'll verify converter.py BEFORE building the scraper. You don't need
a fancy test framework to start — even a plain script that builds a deck from a few
hardcoded Cards and writes test.apkg counts. Later you can turn these into real
pytest functions.

TODO (once converter.build_deck exists):
  - build a small list of fake Cards
  - call build_deck(...) to write a .apkg into this folder
  - assert the file was actually created (os.path.exists)
  - then import that .apkg into Anki by hand to eyeball the cards
"""

# from quizlet_to_anki.models import Card
# from quizlet_to_anki.converter import build_deck

# TODO: write your first converter test here.
